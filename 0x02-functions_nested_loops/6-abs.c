#include "main.h"

/**
 * _abs  - compute the absolute value of an integer
 * @n: takes an integer type input from function
 *
 * Return: 0 (Success)
 */

int _abs(int)
{
if (n < 0)
n = (-1) * n;
return (n);
}
