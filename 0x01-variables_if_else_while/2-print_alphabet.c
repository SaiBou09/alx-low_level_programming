#include <stdio.h>

/**
 * main - Entry point
 * Description: Program printing the alphabet in lowercase
 * Return: O (Success)
 */
int main(void)
{
char c
for (c = 'a', c <= 'z', c++)
{
	putchar(c);
}
putchar('\n');
return (0);
}
