#include <stdio.h>
/**
 * main - A programm that prints a name with puts function
 * Return: O (Success)
 */
int main (void)
{
	puts ("\"Programming is like building a multilingual puzzle");
        Return(0);
}
